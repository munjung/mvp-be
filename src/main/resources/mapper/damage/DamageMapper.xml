<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktds.mvp.demo.domain.damage.mapper.DamageMapper">
    <select id="getDamageList" resultType="com.ktds.mvp.demo.domain.damage.dto.DamageDto">
        select
        t1.id as category_id
        , t1."name" as category_name
        , t2.id as part_id
        , t2."name" as part_name
        from damage_category t1
        inner join damage_part t2
        on t1.id = t2.category_id
        order by t1.id, t2.id
    </select>
</mapper>